server.port=8081
spring.application.name=user-service

# ---- Database (matches your existing ConnectDoc schema) ----
spring.datasource.url=jdbc:mysql://localhost:3306/p25_ctyd?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Eshwar@sql123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ddl-auto=validate -> Hibernate will NOT alter your existing tables, only checks entities match them.
# Use 'update' temporarily only if you want Hibernate to create the tables for you.
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# ---- JWT ----
jwt.secret=ConnectDocSuperSecretKeyForJWTSigningChangeThisInProduction123456
jwt.expiration-ms=86400000

# ---- CORS (React dev server) ----
app.cors.allowed-origins=http://localhost:5173,http://localhost:3000
