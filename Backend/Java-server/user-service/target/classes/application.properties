spring.application.name=user-service
server.port=8081

# MySQL Database configuration (shared schema, this service only owns USERS + ROLES tables)
spring.datasource.url=jdbc:mysql://localhost:3306/p25_ctyd?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Eshwar@sql123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT signing - this SAME secret must be copied into doctor-service and
# appointment-service's application.properties so they can validate tokens
# issued here. In a real deployment, move this to an env var, not source control.
jwt.secret=ConnectToYourDoctorSuperSecretKeyForJWTSigning2026ChangeMe
jwt.expiration-ms=86400000

# --- Eureka Discovery Client ---
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.instance.prefer-ip-address=true
